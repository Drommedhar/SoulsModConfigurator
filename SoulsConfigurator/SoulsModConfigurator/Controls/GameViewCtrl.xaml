<UserControl x:Class="SoulsModConfigurator.Controls.GameViewCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SoulsModConfigurator.Controls"
             xmlns:ui="http://schemas.modernwpf.com/2019" 
             xmlns:controls="clr-namespace:SoulsModConfigurator.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch" Height="Auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid HorizontalAlignment="Stretch" Grid.Row="0" Width="Auto" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="D:\git\SoulsModConfigurator\SoulsConfigurator\SoulsConfigurator\bin\Debug\net8.0-windows" 
           VerticalAlignment="Center" HorizontalAlignment="Stretch" IsReadOnly="True" Grid.Column="0"/>
            <Button Content="Browse..." 
                    Margin="5" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Right"
                    Grid.Column="1"/>
        </Grid>
        <Separator Grid.Row="1" Margin="5" Background="#FF222225"/>
        <controls:ModListCtrl Grid.Row="2"/>

        <Separator Grid.Row="3" Margin="5" Background="#FF222225"/>
        <ui:SimpleStackPanel Orientation="Horizontal" Spacing="5" Grid.Row="4" VerticalAlignment="Bottom" Margin="5,0,5,5">
            <Button Content="Install selected Mods" 
                    Margin="5"
                    IsEnabled="{Binding IsDirty, Mode=OneWay}"/>
            <Button Content="Remove all Mods" 
                    Margin="5"
                    IsEnabled="{Binding IsDirty, Mode=OneWay}"/>
        </ui:SimpleStackPanel>
    </Grid>
</UserControl>
